# Shared Task Notes

## Current endpoints
- `GET /` - Hello World
- `GET /fortune` - Random developer fortune
- `GET /ping` - Health check with uptime
- `GET /echo?message=text` - Echo with reverse (e.g., `/echo?message=hello`)

## To test
```bash
npm start
# Visit http://localhost:3000/echo?message=hello
```

## Project status
Simple webserver is complete with 4 endpoints. Ready for use.
